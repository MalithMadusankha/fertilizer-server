{
    "build": {
        "start": "apt update && apt install -y libgl1 libglib2.0-0 libsm6 libxrender1 libxext6 && pip install --no-cache-dir -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port $PORT"
    },
    "deploy": {
        "commands": [
            "echo 'Deployment Successful!'"
        ]
    }
}
